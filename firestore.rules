rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access — no Firebase Auth is configured for this deployment.
    // Restrict this once authentication is added.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
